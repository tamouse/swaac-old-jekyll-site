{
  "name": "swaac.tamouse.org",
  "version": "0.0.0",
  "description": "My awesome site",
  "main": "gulpfile.js",
  "author": "Me",
  "license": "MIT",
  "repository": "tamouse/drink_up_doctor",
  "devDependencies": {
    "browser-sync": "^2.7.12",
    "gulp": "^3.9.0",
    "gulp-bower": "0.0.10",
    "gulp-compass": "^2.1.0",
    "gulp-minify-css": "^1.2.0",
    "gulp-notify": "^2.2.0",
    "gulp-replace": "^0.5.4",
    "gulp-ruby-sass": "^1.0.5",
    "gulp-task-listing": "^1.0.1"
  },
  "scripts": {
    "start": "gulp",
    "build": "gulp dist",
    "deploy": "gulp dist && git add --all && git commit -m publish && git push -f deploy master",
    "publish": "npm run deploy"
  }
}
